name: webmcp

on:
  push:
    branches: [main]
    paths: ['webmcp/**', 'web/src/data/atlas/**', 'jest.config.js', 'package.json', 'package-lock.json', '.github/workflows/webmcp.yml']
  pull_request:
    paths: ['webmcp/**', 'web/src/data/atlas/**', 'jest.config.js', 'package.json', 'package-lock.json', '.github/workflows/webmcp.yml']

permissions:
  contents: read

concurrency:
  group: webmcp-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: test + example drift
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: package-lock.json

      - run: npm ci

      # The jest slice covers the generator, the browser runtime (jsdom), and
      # every committed example bundle (regenerated and diffed — drift in
      # examples/ fails here; regenerate with sightmap-webmcp generate). The
      # atlas paths trigger because the example bundles hash those corpora.
      - run: npx jest webmcp
