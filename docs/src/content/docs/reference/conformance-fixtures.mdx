---
title: "Conformance fixtures"
description: "The fixture catalog, how to run, how to add."
---

The conformance fixtures are language-agnostic test cases. Every Sightmap implementation is expected to pass them. They live under [`spec/conformance/`](https://github.com/sightmap/sightmap/tree/main/spec/conformance).

## Layout

Each fixture is a directory named `NNN-{slug}.fixture/` containing:

- `sightmap/` — input YAML files (the simulated `.sightmap/` directory)
- `expected.json` — a list of `{ command, args, expected }` test cases the runner checks

The runner asserts that every key in `expected` is present in the actual output and matches deeply. Extra keys in the actual output are allowed, so the suite is forward-compatible. For arrays in `expected`, the actual array must be at least as long, and the prefix must match. The exact shape is documented in [`spec/conformance/README.md`](https://github.com/sightmap/sightmap/blob/main/spec/conformance/README.md).

## Current fixtures

| # | Name | Exercises |
|---|---|---|
| 001 | `minimal` | Smallest valid sightmap; basic `match` |
| 002 | `multi-file-merge` | Same view name across two files → `merge-collision-view` warning |
| 003 | `route-precedence` | Most-specific-wins: literal &gt; `:param` &gt; `*` &gt; `**`; declaration order tie-breaks |
| 004 | `param-normalization` | Express-style `:param` normalizes to `*` |
| 005 | `selector-array` | `selector` accepts an array; alternates tried in order |
| 006 | `view-scoped-vs-global` | Global components match everywhere; scoped only on their view |
| 007 | `request-method-filter` | `match` filters requests by HTTP method |
| 008 | `dependencies-binding` | `dependencies[]` glob binding and diagnostics (SEP-0001) |
| 010 | `component-ref` | `$ref` expansion, view attestation, global+view dedup (SEP-0002) |
| 011 | `component-ref-unresolved` | `$ref` to an unknown component → `ref-unresolved` error |
| 012 | `component-ref-circular` | Self-referential `$ref` chain → `ref-circular` error |

A second series (`100-fmt-*`) covers formatter behavior — quoting, key order, list sort, comment and header preservation, idempotence, and leaving invalid input untouched.

## Running locally

The fixture *inputs* validate against the JSON Schema with the repo's checker:

```bash
# from a checkout of sightmap/sightmap
cd spec && npm install && node scripts/validate-sightmap.mjs conformance
```

The full `{ command, args, expected }` assertions are executed by a conforming implementation's test suite — the reference implementation lives under [`go/`](https://github.com/sightmap/sightmap/tree/main/go). A port in another language implements a runner that loads each fixture's `sightmap/` directory, executes the listed `command` with `args`, and asserts the result matches `expected`.

## Contributing a new fixture

1. Create the next-numbered `NNN-{slug}.fixture/` directory under `spec/conformance/` (see [conventions](https://github.com/sightmap/sightmap/blob/main/spec/CONVENTIONS.md)).
2. Author `sightmap/*.yaml` and `expected.json`. Keep each fixture focused on one behavior.
3. Verify it passes, then open a PR. Sign off your commits with `-s` per the [Contributing guide](/reference/contributing/).
