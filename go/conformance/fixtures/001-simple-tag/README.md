# 001-simple-tag

A minimal tree containing a single `button` element nested inside a `div` root. The sightmap defines one component (`Button`) with the selector `button`. This fixture verifies that the most basic case works: a single tag-name selector matches exactly the one node in the tree that carries that tag, and that the root `div` node is correctly excluded.
