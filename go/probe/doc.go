// Package probe embeds the canonical cdp-probe.js, the browser-side component
// extractor. Downstream consumers embed the probe script from here so
// extraction logic cannot diverge across implementations.
package probe
