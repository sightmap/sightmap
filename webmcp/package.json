{
  "name": "sightmap-webmcp-runtime",
  "version": "0.1.0",
  "private": true,
  "description": "Canonical browser runtime for sightmap webmcp bundles, plus jsdom tests. The generator itself is `sightmap webmcp` in the Go CLI."
}
