# OS / editor cruft
.DS_Store

# Local agent tooling — never committed
.yaks/
.agents/
.claude/

# Temporary conversion roadmap — local only, removed when the merge is done
/PLAN.md

# Node / JS (web/)
node_modules/
dist/
web/src/generated/
# Generated by web/scripts/build-atlas.ts from the vendored src/data/atlas/
# tree — screenshots plus the /atlas/index.json and /atlas/<slug>.md machine
# twins. Wiped and rewritten on every build so an entry removed upstream
# (a takedown) actually disappears from dist/.
web/public/atlas/

# npm release packaging staging (go/npm/scripts/build-npm-packages.mjs)
/npm-staging/
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment
.env
.env.*
*.local

# Go (go/) — local build output (binaries only; NOT the go/sightmap package dir)
/sightmap
go/cmd/sightmap/sightmap
go/cmd/sightmap/sightmap-go

# Local Chrome session state (go/ browser)
.session

# npm wrapper: downloaded binary
go/npm/vendor/

# Basis transcoder, copied from the installed three package by
# web/scripts/copy-basis-transcoder.ts so it cannot drift from three.
web/public/basis/
